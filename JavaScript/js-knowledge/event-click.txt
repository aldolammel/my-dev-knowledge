

EVENT: CLICK


    >> How to change a bool value through a click trigger:
        click="product.active = !product.active
        
    >> This "!" above means "the opposite", so above I'm saying the
        'product.active' attribute is after the click exactly its
        opposite current value.

