

JAVASCRIPT ENVIRONMENT VARIABLE MANAGER: DOTENV

    xxxxxx.

    Examples (Do not use then):
        /JavaScript/Env-Var-Managers/dotenv/examples/.env
        /JavaScript/Env-Var-Managers/dotenv/examples/example.js

    >> Installing:

        1) Add to the front-end project as development dependency:
            
            $ npm install -D dotenv

            # Check the front-end folder's file 'package.json' to see if everything is fine;

        2) 

