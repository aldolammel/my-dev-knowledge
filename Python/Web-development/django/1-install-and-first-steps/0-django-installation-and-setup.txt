

DJANGO: INSTALLING AND SETUP


    1) Which scenario is your case:

        1A) This machine and IDE are ready for Django projects, and I got a brand new project:

            >> Skip this file, jumping to this one:
                ./1-install-new-project.txt


        1B) This machine and IDE are ready for Django projects, and I'm just re-installing Django for an existing app (or re-installing the entire project):
            
            >> Skip this file, jumping to this one:
                ./1-reinstall-new-copy.txt


        1C) This machine (or IDE) is new (for example, after the OS installation) and I need to prepare it to run Django projects:

            >> Keep yourself in this roadmap file!


        - - - - -


        1C) This machine (or IDE) is new! Preparing it to everything about Django - - - - - - - - -

            1C.PRE.1) Python interpreter (if needed);
                /Python/1-python-installation/

            1C.PRE.2) Create the project folder, and get it:
                $ mkdir <project_folder>
                $ cd <project_folder>

            1C.PRE.3) DON'T create the .venv yet (so is NOT need to activate environ for now)!

            1C.PRE.4) IDE, language setup:
                >> VSCode:  /Python/IDE-softwares/vscode/basic-for-python.txt
                >> PyCharm: /Python/IDE-softwares/pycharm/basic-for-python.txt

            1C.1) IDE, framework setup:
                >> VSCode:  .../django/IDE-softwares/vscode/basic-for-django.txt
                >> PyCharm: .../django/IDE-softwares/pycharm/basic-for-django.txt

            1C.2) Install Django for a specific project:

                >> Using UV (recommended, smarter):
                    ./2-install-project-with-uv.txt

                >> Or using PIP (Django built-in solution):
                    ./2-install-project-with-pip.txt


        - - - - -

    
    2) Once Django project already installed, let's ask us again about the current case:
    
        A) This machine and IDE are already for Django projects, and I got a brand new project:

            >> Skip this file, jumping to this one:
                ./1-install-new-project.txt

        B) This machine and IDE are ready for Django projects, and I'm just re-installing Django for an existing app (or re-installing the entire project):
            
            >> Skip this file, jumping to this one:
                ./1-reinstall-new-copy.txt



- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 


    MAKE SURE YOU ALREADY FINISHED ONE OF THESE ROADMAPS:

        >> DJANGO as back and front-end:
            .../django/0-new-project/web-project-django-only.txt

        >> Django with VUE as front-end:
            .../django/0-new-project/web-project-django-with-vue.txt

        >> Django with REACT as front-end:
            .../django/0-new-project/web-project-django-with-react.txt

        >> Django with ANGULAR as front-end:
            .../django/0-new-project/xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

