

    POSTGRESQL: LISTING DATABASE AVAILABLE


        >> Windows:

            $ psql -U postgres -c "\l"

            >> To show the database port:

                $ psql -U postgres
                $ SHOW port;              <-- the ';' is needed!



        >> Ubuntu:

            $ sudo -i -u postgres
            $ psql -c "\l"

            >> To show the database port:
                $ xxxxx
                $ xxxxx



        >> Mac: 

            $ sudo -i -u postgres
            $ psql -c "\l"

            >> To show the database port:
                $ xxxxx
                $ xxxx