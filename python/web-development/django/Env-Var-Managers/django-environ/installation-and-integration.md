

DJANGO-ENVIRON: INSTALLATION

    

    >> Installation:

        1) Include the manager as a mandatory project's dependency:
        
            >> Using UV:
                $ uv add django-environ
            
            >> Or using PIP:
                $ python3 -m pip install django-environ


    >> Integration:

        1) (If applicable)
            Make sure .gitignore file is ignoring the backend .env file;