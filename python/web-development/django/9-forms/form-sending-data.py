''' 

    FORMS: GETTING DATA

        >> Form sends data to Model;

    >> Getting data:
        ./form-getting-data.py
'''

# AT FIRST: To send data (updated data) to the model, you generally don't need any special approach. If the form already has the data (when the field you need's already in form's 'fields' array) just call the data using self.cleaned_data['fieldname']. 

