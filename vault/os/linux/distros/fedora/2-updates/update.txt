

FEDORA: UPDATING THE SYSTEM

    $ sudo dnf update        ← update the repositories list
    $ sudo dnf upgrade       ← install the available updates
    $ sudo dnf autoremove    ← removes what’s not needed anymore.   ???????????????????????
    $ sudo dnf autoclean    ← cleans the local package cache by removing downloaded obsolete files.???????????????????????
