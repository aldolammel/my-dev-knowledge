"""
CREATING A VIRTUAL ENVIRONMENT:

1) GO to the terminal;
2) Go to the folder that you want to install the virtual environment;
3) In the folder, type through Terminal to create the virtual environment: virtualenv venv
    If the command wasn't recognized, install the virtualenv module: pip install virtualenv

"""