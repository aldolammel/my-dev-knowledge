

DEBIAN/UBUNTU: FINDING INSTALLED APPS

        # Finding all:
            $ apt list --installed
        
        # Finding all with 'adobe' in the name:
        
            $ apt list --installed | grep adobe

        # Finding all the initial letters are 'web':
        
            $ apt list --installed | grep ^web