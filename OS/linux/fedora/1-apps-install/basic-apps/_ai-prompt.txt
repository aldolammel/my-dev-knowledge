
PRE) Current system:
    $ lsb_release -a <see equivalent for Fedora>


>> Best prompt to install things:

    Shortly, I'm on Fedora <XX.XX>.
    Install <app-name>.
    Requirements:
    - By terminal and using DNF.
    - Use official repository or trusted PPA if necessary.

    Provide exact commands only!