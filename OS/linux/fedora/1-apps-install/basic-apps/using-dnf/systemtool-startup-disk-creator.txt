

SYSTEM TOOL: STARTUP DISK CREATOR

    It mounts .iso in USB driver to, for example, use it as a bootable disk during formatting the machine.

    1) Installing:
        $ sudo apt install -y usb-creator-gtk