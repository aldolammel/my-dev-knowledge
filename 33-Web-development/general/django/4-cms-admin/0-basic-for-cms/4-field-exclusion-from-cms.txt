

FIELD EXCLUSION FROM THE CMS:

    >> To make a field/attribute be available only in the database, 
        go to the sub-app 'admin.py' file and:

            class MovieCMS(admin.ModelAdmin):
                exclude = ('full_name',)
                